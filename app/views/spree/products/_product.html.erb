<% url = spree.product_path(product, taxon_id: taxon.try(:id)) %>
<div id="product_<%= product.id %>" class="d-flex w-100 col-12 col-sm-6 col-lg-4 col-xl-3 product-list-item mb-4" data-hook="products_list_item">
  <div class="w-100 product-card">
    <% cache(taxon.present? ? [I18n.locale, current_currency, taxon, product] : cache_key_for_product(product)) do %>
      <div class="card-body product-body">
        <%= link_to url, class: 'd-block' do %>
          <%= small_image(product, class: "d-block mx-auto pb-4") %>
          <%= content_tag(:span, truncate(product.name, length: 50), class: 'info mt-3 d-block product-card-text', title: product.name) %>
          <div class="product-card-text">
            <span>
              <span class="price selling lead">
                <%= display_price(product) %>
              </span>
            </span>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
